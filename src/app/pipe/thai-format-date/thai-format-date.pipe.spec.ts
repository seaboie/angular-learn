import { ThaiFormatDatePipe } from './thai-format-date.pipe';

describe('ThaiFormatDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ThaiFormatDatePipe();
    expect(pipe).toBeTruthy();
  });
});
